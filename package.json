{
  "name": "purescript-halogen-helix",
  "description": "Lightweight global state management for PureScript Halogen.",
  "version": "1.0.0",
  "author": {
    "email": "katsujukou@gmail.com",
    "name": "Katsujukou Kineya"
  },
  "license": "MIT",
  "keywords": [
    "purescript",
    "halogen",
    "store",
    "state management",
    "global state"
  ],
  "scripts": {
    "build": "spago build",
    "test": "npm run test:unit && npm run test:E2E",
    "test:unit": "spago test -m Test.Unit.Main",
    "test:E2E": "spago test -m Test.E2E.Main",
    "example": "npm run example:build && cd example && vite serve",
    "example:build": "spago build -p example && cd example && vite build",
    "check": "purs-tidy check src test example/src",
    "format": "purs-tidy format-in-place src test example/src"
  },
  "deependencies": {},
  "devDependencies": {
    "happy-dom": "^17.4.4",
    "uuid": "^11.1.0",
    "vite": "^6.2.6"
  }
}
